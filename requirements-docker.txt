# ============================================================================
# DOCKER REQUIREMENTS (TensorFlow excluded for smaller images)
# ============================================================================
# This file is used by Docker containers
# TensorFlow is excluded to reduce image size (~475MB savings)
# Use requirements.txt for local development (includes TensorFlow)
# ============================================================================

# Core ML libraries
scikit-learn==1.3.2          # ML models (RandomForest, GradientBoosting, etc.)
pandas==2.1.3                 # Data manipulation
numpy==1.24.3                 # Numerical computing

# Model serialization
joblib==1.3.2                 # Better than pickle for sklearn models

# Data processing
kaggle==1.5.16                # Kaggle API client for dataset download

# Visualization (Training container only)
matplotlib==3.8.2             # Plotting
seaborn==0.13.0               # Statistical visualizations
jupyter==1.0.0                # Notebooks for EDA

# Utilities
python-dotenv==1.0.0          # Environment variable management

# NOTE: TensorFlow is NOT included here
# If you need neural network in Docker, install it separately or use requirements.txt
