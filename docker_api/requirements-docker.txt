# FastAPI Backend Dependencies (Docker - TensorFlow excluded)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# ML Dependencies (sklearn models only)
scikit-learn==1.3.2
pandas==2.1.3
numpy==1.24.3
joblib==1.3.2

# NOTE: TensorFlow is NOT included here
# API only supports sklearn models when TensorFlow is excluded
# If you need Keras model support, use requirements.txt instead
